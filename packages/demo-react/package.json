{
  "name": "@genoacms/demo-react",
  "version": "0.0.0",
  "private": true,
  "description": "A React consumer rendering a GenoaCMS page through @genoacms/sdk",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @genoacms/sdk run build && pnpm --filter @genoacms/demo-support run build && vite",
    "build": "pnpm --filter @genoacms/sdk run build && pnpm --filter @genoacms/demo-support run build && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@genoacms/sdk": "workspace:*",
    "@genoacms/demo-support": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
